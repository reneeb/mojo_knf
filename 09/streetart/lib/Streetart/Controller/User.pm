package Streetart::Controller::User;
use Mojo::Base 'Mojolicious::Controller';

# This action will render a template
sub upload {
  my $self = shift;

  $self->render();
}

sub upload_do {
  my $self = shift;

  $self->render();
}

sub comment {
  my $self = shift;

  $self->render();
}

sub comment {
  my $self = shift;

  $self->render();
}

1;
